# EnjoyAgent Frontend Plan

## 当前状态

前端已经完成了阶段 1、阶段 2、阶段 3、阶段 4 和阶段 5 的主体工作，可以看作是“应用壳、认证、基础配置页、真实聊天工作台和真实知识库模块都已经落地”的状态。

### 已实现功能

#### 1. 基础工程与运行环境

- 已完成 `Vue 3 + TypeScript + Vite` 初始化
- 已接入 `Vue Router`
- 已接入 `Pinia`
- 已接入 `Axios`
- 已接入 `Tailwind CSS`
- 已建立基础目录结构
- 已配置 Vite 代理，前端可直接通过 `/api/**` 联调后端

#### 2. 应用壳与设计系统雏形

- 已完成 Dify 风格的整体布局骨架
- 已完成顶部导航
- 已完成左侧导航
- 已完成工作台主内容区容器
- 已完成认证页布局
- 已完成基础 UI 组件：
  - `UiButton`
  - `UiTextField`
  - `SectionCard`

#### 3. 认证链路

- 已接通注册接口 `/api/auth/register`
- 已接通登录接口 `/api/auth/login`
- 已接通当前用户接口 `/api/auth/me`
- 已完成本地 token 持久化
- 已完成页面刷新后的登录态恢复
- 已完成登出逻辑

#### 4. 路由与权限控制

- 已完成访客页和受保护页面分流
- 已完成未登录跳转登录页
- 已完成管理员页面权限判断
- 已完成普通用户访问管理页时的重定向
- 已完成 404 页面

#### 5. 当前可访问页面

- `/login`
- `/register`
- `/app/home`
- `/app/credentials`
- `/app/model-configs`
- `/app/official-models`
- `/app/agents`
- `/app/knowledge`
- `/app/chat/workspace`
- `/app/admin/overview`

#### 6. 阶段 3 已完成的真实业务页

下面这些页面已经接入真实后端接口，不再是静态预览：

- 凭证管理页
- 模型配置页
- 官方模型列表页
- Agent 列表与创建编辑页

当前已经打通的真实主链是：

`登录 -> 创建凭证 -> 创建模型 -> 创建 Agent -> 编辑 Agent`

并且已经通过前后端联调验证。

#### 7. 阶段 4 已完成的真实聊天页

下面这部分已经接入真实后端接口，不再是静态预览：

- 聊天工作台页
- 会话列表
- 新建会话
- 消息历史加载
- SSE 流式输出
- 运行时调试面板

当前已经打通的真实主链扩展为：

`登录 -> 创建凭证 -> 创建模型 -> 创建 Agent -> 新建会话 -> 流式聊天`

并且已经通过前后端联调验证，联调时走的是前端开发服务器代理链路。

#### 8. 阶段 5 已完成的真实知识库页

下面这部分已经接入真实后端接口，不再是静态预览：

- 知识库列表页
- 创建知识库
- 编辑知识库
- 删除知识库
- 文档上传
- 文档列表与状态展示
- 文档删除
- 文档重建检索索引
- 从知识库跳转到 Agent 绑定入口
- Agent 页知识库绑定

当前已经打通的真实主链扩展为：

`登录 -> 创建凭证 -> 创建模型 -> 创建知识库 -> 上传文档 -> 创建/编辑 Agent 并绑定知识库 -> 聊天工作台发起 RAG 对话`

并且已经完成真实联调验证。

#### 9. 当前页面性质

下面这些页面现在是“高保真静态预览 + 正式应用壳”，还没有接真实业务数据：

- 工作台首页
- 管理后台概览页

也就是说：

- 认证是已经接真实后端的
- 页面布局和视觉方向已经基本定型
- 阶段三的核心 CRUD 已经开始铺开
- 还未真实化的重点会转向 MCP、市场和管理后台

---

## 当前还没实现的核心功能

### 用户工作台侧

- 知识库列表、详情、文档列表、上传
- MCP Server 列表、详情、工具绑定
- 钱包余额、流水、充值单
- 市场列表、市场详情、提交流程、安装流程

### 管理后台侧

- 官方模型凭证管理
- 官方模型配置管理
- 充值单审核
- 市场资产审核
- 运营统计与计费审计

### 工程能力

- 统一错误页和空状态
- 列表分页、筛选、搜索
- 表单校验体系统一
- 组件级测试
- 端到端测试

---

## 分阶段推进路线

## 阶段 3：凭证、模型、Agent 基础管理

### 目标

让用户能在前端完成第一条真实配置链路：

`创建凭证 -> 创建模型 -> 创建 Agent`

### 已完成

- 凭证列表页
- 创建凭证弹窗或抽屉
- 用户模型配置列表页
- 创建模型配置表单
- 官方模型列表页
- Agent 列表页
- Agent 创建页
- Agent 编辑页

### 已接通接口

- 凭证相关接口
- 用户模型配置相关接口
- 官方模型查询接口
- Agent 列表与创建接口

### 完成结果

- 用户可以在前端创建、编辑、删除自己的凭证
- 用户可以配置聊天模型、Embedding 模型和 Rerank 模型
- 用户可以查看官方模型列表
- 用户可以创建、编辑、删除基础 Agent
- 阶段三主链已完成前后端联调

---

## 阶段 4：聊天工作台真实化

### 目标

把当前高保真聊天预览页，升级成真实可聊天的工作台。

### 已完成

- 会话列表
- 新建会话
- 消息历史加载
- 输入框发送
- SSE 流式输出
- 错误提示
- 消息加载状态
- 右侧调试面板数据接入
- 模型来源和知识命中信息展示

### 验收结果

- 用户已经可以在前端真正与 Agent 对话
- 流式返回已经正常显示
- 对话历史刷新后会保留
- 右侧调试面板已经可以显示基础运行信息
- 已完成真实前后端联调
- 已修复后端 `SSE` 异步 dispatch 被 Spring Security 拦截的问题
- 工具调用轨迹可视化继续放到阶段 6 和 MCP 模块一起补

---

## 阶段 5：知识库模块

### 目标

把 RAG 的前端管理能力真正做起来。

### 已完成

- 知识库列表页
- 创建知识库
- 知识库详情页
- 文档列表
- 上传文档
- 文档状态展示
- 知识库导入向导真实接入
- Agent 与知识库绑定入口
- Agent 编辑页知识库绑定
- 工作区与聊天页滚动体验优化

### 验收结果

- 用户已经可以创建、编辑和删除知识库
- 用户已经可以上传文档并看到真实状态变化
- 用户已经可以删除文档和重建检索索引
- 用户已经可以把知识库绑定到 Agent
- 知识库阶段已完成真实前后端联调

---

## 阶段 6：MCP 模块

### 目标

把 MCP Server、工具定义和 Agent 工具绑定接起来。

### 需要完成

- MCP Server 列表页
- MCP Server 创建与编辑页
- Tool 列表
- Agent 工具绑定区
- OAuth 状态页
- 需要认证的 MCP 的引导态

### 验收标准

- 用户可以创建 MCP Server
- 用户可以查看和管理工具定义
- 用户可以把 MCP 工具绑定到 Agent

---

## 阶段 7：钱包、充值与共享市场

### 目标

把平台能力接到用户工作台里。

### 需要完成

- 钱包余额页
- 钱包流水页
- 充值单列表
- 创建充值单
- 市场列表页
- 市场详情页
- 提交到市场
- 安装市场资产
- 安装结果页

### 验收标准

- 用户能看到余额和流水
- 用户能提交充值单
- 用户能浏览市场并安装资产

---

## 阶段 8：管理后台

### 目标

把管理员真实工作流补齐。

### 需要完成

- 官方模型凭证管理页
- 官方模型配置管理页
- 充值单审核页
- 市场资产审核页
- 运营概览页真实数据接入
- 计费事件或流水审计页

### 验收标准

- 管理员能完成官方模型配置
- 管理员能审核充值单
- 管理员能审核市场资产

---

## 阶段 9：工程化与体验收尾

### 目标

把前端从“功能能跑”推到“结构稳定、体验完整”。

### 需要完成

- 全局空状态、错误状态统一
- 骨架屏和加载体验统一
- 表格和表单抽象收口
- 关键页面测试
- 关键主链 E2E
- 文档收口

---

## 下一步准备做什么

下一步我建议直接开始 **阶段 5：知识库模块**。

原因很简单：

- Agent 的配置和对话主链已经打通
- 现在最值得优先兑现的是 RAG 的前端管理能力
- 知识库模块会直接决定这个产品的差异化体验是否完整

### 下一步的具体落点

我建议按这个顺序做：

1. 接知识库列表和创建知识库
2. 接知识库详情和文档列表
3. 接真实上传文档
4. 接文档状态和处理进度展示
5. 把当前知识库导入预览页升级成真实导入向导

### 下一步完成后的结果

完成这一轮后，前端就会具备第一条完整的 RAG 使用主链：

`注册/登录 -> 创建凭证 -> 创建模型 -> 创建知识库 -> 上传文档 -> 绑定 Agent`

这会让项目从“能配置 Agent”进入“能真正使用 Agent”的阶段。

---

## 说明

当前 UI 风格已经确定为：

- 中文为主
- Dify 风格的轻工作台
- 不走传统重后台模板

后续所有页面都应继续遵循：

- 统一应用壳
- 统一卡片、表单、抽屉与列表样式
- 优先沿用现有布局和设计语言
