# EnjoyAgent Frontend UI Style

## 目标

这份文档用于在前端正式开工前，把整体 UI 风格、布局体系、组件路线和设计约束先定下来。

你的要求很明确：

- 界面风格参考 Dify
- 一开始就固定 UI 方向
- 后续逐阶段补功能，但不推翻整体视觉体系
- 可以使用成熟 UI 方案来保证页面质感

所以这份文档的核心结论是：

`前端先做设计系统和应用壳，再做业务页面。`

## 最终风格定位

EnjoyAgent 前端建议采用：

`Dify 风格的轻 SaaS 工作台 + 现代 AI 产品界面`

风格关键词：

- 干净
- 明亮
- 留白充足
- 中性色为主
- 蓝色强调
- 圆角柔和
- 边框轻
- 阴影克制
- 信息密度中等
- 更像 AI 工作台，而不是传统后台

不建议走下面这些方向：

- 很重的企业后台风
- 很深色的 hacker 风
- 花哨渐变首页风
- 过度卡片化导致页面碎片化

## 推荐 UI 技术栈

如果要做出接近 Dify 的气质，我建议前端 UI 层采用这套组合：

- Tailwind CSS
- `shadcn-vue`
- Radix Vue
- Lucide Icons
- TanStack Table

结合你前面已经定下来的基础栈，完整推荐为：

- Vue 3
- TypeScript
- Vite
- Vue Router
- Pinia
- TanStack Vue Query
- Axios
- Tailwind CSS
- `shadcn-vue`
- Radix Vue
- Lucide Vue Next
- TanStack Table
- `vee-validate` + `zod`
- `@vueuse/core`

## 为什么我推荐这套，而不是直接上重型后台组件库

如果你要的是 Dify 风格，最不适合的方案其实是“拿一套传统后台组件库直接铺满”。

原因：

- Dify 的页面不是典型的企业 ERP 风格
- 聊天页、知识库导入页、市场页都需要较强的定制布局
- 重型后台 UI 框架容易把页面做成统一但呆板的表格后台

`shadcn-vue + Tailwind` 更适合的原因是：

- 风格现代
- 很容易做出轻盈、圆润、克制的 SaaS 页面
- 组件基础足够好看
- 但不会把布局锁死

我的建议是：

- 主 UI 体系用 `shadcn-vue`
- 数据密集页用 TanStack Table
- 不要一开始混用两套大型 UI 框架

## 视觉基线

### 颜色

建议采用一套偏 Dify 的浅色主题：

- 主背景：暖白或极浅灰白
- 卡片背景：纯白
- 主文本：深灰而不是纯黑
- 次级文本：中灰
- 分割线：非常浅的灰
- 品牌强调色：偏清爽的蓝色
- 成功：柔和绿色
- 警告：琥珀色
- 危险：偏红但不过饱和

建议不要：

- 紫色作为主品牌色
- 强对比黑白
- 高饱和蓝紫渐变大面积铺底

### 圆角

建议统一走中等圆角：

- 按钮：`10px` 左右
- 输入框：`12px`
- 卡片：`16px`
- 对话面板和弹层：`16px ~ 20px`

### 阴影

建议非常克制：

- 常规卡片几乎不靠大阴影
- 更多依赖浅边框和轻背景层次
- 浮层再加轻阴影

### 字体与排版

建议使用：

- 英文/数字：`Inter`
- 中文：`PingFang SC` / `Microsoft YaHei` / 系统中文字体回退

排版建议：

- 页面标题清晰但不要太巨大
- 描述文字偏短，避免后台页面长段说明
- 表单区块和导入流程要比列表页更宽松

## 布局体系

为了贴近 Dify，我建议从一开始就固定 4 类页面骨架。

### 1. 应用工作台骨架

适用页面：

- Agent 列表
- 聊天页
- 模型配置页
- 钱包页

布局建议：

- 顶部全局导航
- 左侧一级导航
- 主内容区自适应

特点：

- 顶栏很轻
- 左侧导航不宜过宽
- 内容区边距统一

### 2. 资源管理页骨架

适用页面：

- 知识库列表
- MCP Server 列表
- 市场列表
- 充值单列表

布局建议：

- 页面标题区
- 筛选/搜索/操作区
- 表格或卡片列表区
- 右侧抽屉或弹层承载编辑

### 3. 聊天工作台骨架

适用页面：

- Agent 聊天

布局建议：

- 左侧：会话列表
- 中间：消息流
- 右侧：调试/知识命中/工具调用信息面板

这块非常关键，因为它决定产品第一印象。

建议：

- 中间消息区尽量宽
- 输入框区域做成产品级而不是后台表单级
- 调试面板可以折叠

### 4. 向导式流程骨架

适用页面：

- 知识库导入
- 市场安装
- MCP OAuth 配置

布局建议：

- 页面顶部步骤条
- 中间主体卡片
- 下方操作按钮区

这类页面要非常接近你给的 Dify 参考图：

- 清晰步骤
- 大块留白
- 中央聚焦
- 明确主操作按钮

## 组件层级建议

从一开始就建议分成 3 层：

### 1. 基础组件层

基于 `shadcn-vue` 封装：

- Button
- Input
- Select
- Dialog
- Drawer
- Dropdown
- Tabs
- Tooltip
- Badge
- Table

### 2. 产品组件层

面向 EnjoyAgent 业务抽象：

- AppShell
- SidebarNav
- PageHeader
- EmptyState
- LoadingBlock
- ChatMessage
- ChatComposer
- KnowledgeUploadDropzone
- BillingStatCard
- MarketAssetCard
- SetupRequiredBadge

### 3. 页面模板层

复用页面布局：

- ListPageTemplate
- DetailPageTemplate
- WizardPageTemplate
- ChatWorkspaceTemplate
- AdminReviewTemplate

## 每个阶段里必须前置完成的 UI 事项

为了避免后期返工，我建议把 UI 的前置工作直接塞进阶段 0。

### 阶段 0A：设计基线

必须先完成：

- 颜色变量
- 字体方案
- 圆角和阴影规范
- 间距系统
- 全局背景和容器宽度
- 按钮 / 输入框 / 卡片 / 标签的基础样式

完成标准：

- 还没做业务页，但已经能看出产品整体气质

### 阶段 0B：应用壳

必须先完成：

- 顶部导航
- 左侧导航
- 页面标题区
- 全局空状态
- 全局加载状态
- 错误提示样式

完成标准：

- 任何新页面放进去都不会“风格未定义”

### 阶段 0C：页面模板

必须先完成：

- 列表页模板
- 抽屉表单模板
- 向导页模板
- 聊天工作台模板

完成标准：

- 后续做知识库、MCP、市场、管理后台时，只是在既有模板上填业务内容

## 最推荐的页面风格优先级

如果你要做得像 Dify，我建议前端视觉优先级这样排：

1. 顶部导航和应用壳
2. 聊天页
3. 知识库导入向导
4. 资源管理列表页
5. 市场页
6. 管理后台

也就是说：

- 不要先把后台表格做满
- 要先把最能定义产品气质的页面做对

## 对管理后台的建议

管理后台也应该继承同一套设计系统，但密度可以略高。

建议：

- 保持同样的颜色和组件体系
- 只是信息密度更高、功能更偏表格和审核
- 不要做成另一套完全不同的 Admin 模板

这样可以保证：

- 用户工作台和管理后台看起来属于同一个产品
- 维护成本更低

## 结论

如果你要做类似 Dify 的界面，我建议前端从一开始就采用下面这条路线：

`Vue 3 + TypeScript + Vite + Tailwind CSS + shadcn-vue + Radix Vue + TanStack Query + TanStack Table`

同时在功能开发前，先固定：

- 设计 Token
- 应用壳
- 页面模板
- 聊天页和向导页的布局方向

这样后面每做一个模块，都是在同一套视觉系统里自然扩展，而不是最后再返工统一 UI。
