# EnjoyAgent UI Style Guide

EnjoyAgent Frontend 的视觉目标是：**像一个现代 AI SaaS 工作台，而不是传统管理后台**。

整体方向参考 Dify / Coze / Notion AI 这类产品：明亮、轻盈、结构清晰，有足够的产品感，也能承载复杂的 Agent、RAG、MCP、Workflow 和 Billing 信息。

## 设计关键词

- 明亮
- 干净
- 圆润
- 克制
- 信息分区清楚
- 主链路优先
- 中文体验友好
- 更像 AI 工作台，不像 ERP 后台

## 当前视觉基线

### 色彩

- 主背景：极浅蓝灰或白色
- 内容容器：白色
- 主要强调色：清爽蓝
- 成功：柔和绿色
- 警告：琥珀色
- 危险：柔和红色
- 文本：深灰蓝，不使用纯黑作为大面积文本
- 分割线：浅灰蓝，降低视觉噪音

### 圆角

- 按钮：中等圆角或胶囊按钮
- 输入框：中等圆角
- 卡片：偏大圆角
- 主容器：大圆角

当前 UI 已经形成了较明显的轻 SaaS 风格，后续不要突然引入重型后台组件库的默认视觉。

### 阴影

阴影只用于建立层次，不用于制造强烈装饰感。

建议：

- 普通卡片主要靠浅边框
- 浮层、主容器可使用轻阴影
- 不使用大面积渐变或强投影

## 页面骨架

### 1. 用户工作台

适用：

- 工作台首页
- Agent 管理
- 知识库
- MCP 工具
- Workflow
- 钱包
- 共享市场

结构：

- 顶部导航
- 左侧导航
- 中间业务主区域
- 必要时增加右侧详情或调试面板

### 2. 聊天工作台

结构：

- 左侧会话与 Agent 选择
- 中间消息流
- 右侧运行时信息

右侧运行时信息很重要，应该持续展示：

- 模型来源
- 模型名称
- 流式模式
- 耗时
- token
- 知识命中
- MCP 工具调用

### 3. Workflow Builder

结构：

- 左侧节点面板
- 中间无限画布
- 右侧属性和测试面板

设计重点：

- 节点颜色按类型区分
- 连线可见且易操作
- 属性面板不挤压画布
- 测试面板能快速看到执行事件

### 4. 管理后台

管理后台仍沿用同一个视觉系统，但信息密度可以略高。

适用：

- 官方模型管理
- 充值审核
- 市场审核
- 用户管理
- 统计看板

原则：

- 审核动作清晰
- 状态标签明确
- 列表和详情并排，减少来回跳转

## 组件原则

- 优先复用现有 `UiButton`、`UiSelect`、`UiTextField`、`UiTextarea`、`UiCheckbox`、`SectionCard`
- 新组件先服务业务场景，不为了抽象而抽象
- 图标优先使用 `lucide-vue-next`
- 复杂页面优先拆成业务组件
- 表单错误、空状态、加载状态要统一

## 文案原则

- 页面标题短而明确
- 描述文案解释业务价值，不写太多操作说明
- 错误信息尽量可行动，例如“请先选择聊天模型”比“请求失败”更好
- 管理后台文案要明确状态和后果，例如“审核通过后资产会上架到共享市场”

## 不建议的方向

- 大面积紫色或渐变
- 传统重后台模板风
- 过多嵌套卡片
- 每个页面都做成大表格
- 用过度装饰代替清晰信息架构
- 把聊天、市场、Workflow 做得像普通 CRUD 页面

## 后续 UI 待完善

- 统一骨架屏
- 统一空状态
- 统一错误态
- 表单校验视觉统一
- 市场资产详情预览
- Workflow 节点运行态高亮
- 聊天消息引用和工具调用的更精细展示
- 管理后台统计图表
- 移动端适配

## 总结

EnjoyAgent 的前端应该持续保持一个方向：

```text
真实可用的 AI Agent 工作台 + 轻盈现代的产品质感
```

所有新页面都应该先问：这个页面是在帮助用户搭建、运行、调试和运营 Agent，还是只是把接口铺成表格。优先做前者。
